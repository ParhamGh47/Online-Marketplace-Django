A simple Django + Tailwind Online Shop App

Features:
    - Signup/Login
    - Adding New Items (With edit and delete option)
    - Dashboard
    - Search through items
    - Conversations between users

The Tailwind + DaisyUI are there, but not working :( Instead it uses the CDN


<!-- npx tailwindcss -i ./static/src/input.css -o ./static/css/tailwind.css --watch -->